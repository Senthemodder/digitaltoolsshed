{
  "id": "froth-flotation-cell-bank-recovery-kelsall-calculator",
  "name": "Froth Flotation Cell Bank Recovery & Kinetics (Kelsall Two-Rate Model) Calculator",
  "description": "Calculate mineral froth flotation bank recovery, kinetic rate constants, cell residence time, and concentrate mass pull using Kelsall's two-rate fast/slow model across N mechanical flotation cells in series.",
  "category": "Chemical & Mining Engineering",
  "faqs": [
    {
      "q": "What is the Kelsall two-rate flotation model and why is it superior to first-order single-rate kinetics?",
      "a": "Classical first-order flotation kinetics assume all mineral particles float with an identical rate constant k. In real ore flotation, however, particle size distribution, liberation degree, and surface collector coverage create heterogeneous floatability. Kelsall's two-rate model divides the floatable valuable mineral into a fast-floating fraction (1 - Phi_slow) with rate constant k_fast (typically well-liberated, fine-to-medium particles) and a slow-floating fraction Phi_slow with rate constant k_slow (locked composite grains or coarse particles). This dual-rate distribution accurately fits laboratory batch kinetics and reliably predicts bank recovery across rougher, scavenger, and cleaner stages."
    },
    {
      "q": "How does gas holdup affect the true hydrodynamic residence time in flotation cells?",
      "a": "Mechanical flotation cells introduce pressurized or self-aspirated air at superficial gas velocities (Jg) between 0.5 and 2.5 cm/s, creating a continuous dispersed bubble swarm. This gas occupies between 10% and 20% of the active cell volume (gas holdup, epsilon_g). True liquid-solid pulp residence time tau is governed by the net slurry volume: V_slurry = V_cell * (1 - epsilon_g). Ignoring gas holdup overestimates available residence time by 15% to 25%, resulting in undersized flotation tanks, inadequate mineral-bubble collision opportunities, and unrecovered valuable minerals in tailings."
    },
    {
      "q": "What is the mathematical relationship between continuous stirred-tank reactor (CSTR) cells in series and batch flotation time?",
      "a": "A bank of N identical agitated flotation cells operating in continuous mode behaves hydrodynamically as N continuous stirred-tank reactors (CSTRs) in series. For a component with kinetic rate k and mean residence time per cell tau_c, the continuous bank recovery is given by R_bank = 1 - 1 / (1 + k * tau_c)^N. Due to backmixing and RTD dispersion, continuous cells require 1.3 to 1.8 times the nominal batch flotation residence time (the \"scale-up factor\") to reach equivalent metallurgical recovery."
    },
    {
      "q": "How is concentrate mass pull related to feed, concentrate, and tailings grades via two-product formula?",
      "a": "By combining the overall mass balance (F = C + T) with the component balance (F * f = C * c + T * t), the concentrate mass pull percentage (Y = 100 * C / F) is derived as Y = 100 * (f - t) / (c - t). The fractional recovery R is (C * c) / (F * f) = (c / f) * ((f - t) / (c - t)). In reverse, when bank recovery R is calculated from flotation kinetics, the resulting tailings grade is t = f * (1 - R) / (1 - Y) and concentrate pull is Y = 100 * R * (f / c)."
    },
    {
      "q": "What distinguishes rougher, scavenger, and cleaner flotation cell operating parameters?",
      "a": "Rougher banks operate at high throughput to maximize initial recovery of fully liberated fast-floating minerals. Scavenger banks receive rougher tailings to recover difficult slow-floating particles and composite grains, requiring longer residence times, higher collector dosing, and elevated power inputs. Cleaner banks receive rougher/scavenger concentrates to reject entrained gangue and elevate product grade, operating at lower pulp densities (15-22% solids) with deep froth beds and wash-water addition."
    }
  ],
  "content": "\n<div class=\"calc-box\">\n  <div style=\"display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem; flex-wrap: wrap; gap: 1rem;\">\n    <div>\n      <h2 style=\"margin: 0; font-size: 1.5rem; color: var(--primary);\">Froth Flotation Bank Recovery & Kinetics Simulator</h2>\n      <p style=\"margin: 0.25rem 0 0; font-size: 0.9rem; color: var(--text-muted);\">Kelsall Two-Rate CSTR Model • Residence Time Distribution • Mass Pull & Grade Balances</p>\n    </div>\n    <button type=\"button\" class=\"btn-copy\" id=\"btn-copy-flot\" style=\"padding: 0.6rem 1.2rem; font-size: 0.875rem; font-weight: 600; background: var(--surface); border: 1px solid var(--border); border-radius: 6px; cursor: pointer; display: flex; align-items: center; gap: 0.5rem; color: var(--text);\">\n      <span>📋</span> Copy Diagnostic Summary\n    </button>\n  </div>\n\n  <div style=\"display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 1.5rem; margin-bottom: 1.5rem;\">\n    <!-- Feed & Slurry Properties -->\n    <div style=\"background: var(--surface); padding: 1.25rem; border-radius: 8px; border: 1px solid var(--border);\">\n      <h3 style=\"margin-top: 0; font-size: 1.1rem; color: var(--text); border-bottom: 2px solid var(--border); padding-bottom: 0.5rem;\">1. Ore Feed & Slurry Rheology</h3>\n      <div style=\"display: grid; gap: 1rem;\">\n        <div>\n          <label style=\"display: block; font-size: 0.85rem; font-weight: 600; margin-bottom: 0.25rem;\">Dry Solids Feed Rate (t/h)</label>\n          <input type=\"number\" id=\"flot-feed-rate\" value=\"350\" min=\"1\" max=\"10000\" step=\"5\" style=\"width: 100%; padding: 0.5rem; border: 1px solid var(--border); border-radius: 4px; background: var(--bg); color: var(--text);\">\n        </div>\n        <div>\n          <label style=\"display: block; font-size: 0.85rem; font-weight: 600; margin-bottom: 0.25rem;\">Pulp Solids Concentration (wt %)</label>\n          <input type=\"number\" id=\"flot-solids-wt\" value=\"36\" min=\"5\" max=\"65\" step=\"0.5\" style=\"width: 100%; padding: 0.5rem; border: 1px solid var(--border); border-radius: 4px; background: var(--bg); color: var(--text);\">\n        </div>\n        <div style=\"display: grid; grid-template-columns: 1fr 1fr; gap: 0.75rem;\">\n          <div>\n            <label style=\"display: block; font-size: 0.85rem; font-weight: 600; margin-bottom: 0.25rem;\">Solids SG (g/cm³)</label>\n            <input type=\"number\" id=\"flot-sg-solids\" value=\"2.85\" min=\"1.5\" max=\"5.5\" step=\"0.05\" style=\"width: 100%; padding: 0.5rem; border: 1px solid var(--border); border-radius: 4px; background: var(--bg); color: var(--text);\">\n          </div>\n          <div>\n            <label style=\"display: block; font-size: 0.85rem; font-weight: 600; margin-bottom: 0.25rem;\">Liquid SG (g/cm³)</label>\n            <input type=\"number\" id=\"flot-sg-liquid\" value=\"1.00\" min=\"0.9\" max=\"1.3\" step=\"0.01\" style=\"width: 100%; padding: 0.5rem; border: 1px solid var(--border); border-radius: 4px; background: var(--bg); color: var(--text);\">\n          </div>\n        </div>\n        <div style=\"display: grid; grid-template-columns: 1fr 1fr; gap: 0.75rem;\">\n          <div>\n            <label style=\"display: block; font-size: 0.85rem; font-weight: 600; margin-bottom: 0.25rem;\">Feed Grade (%)</label>\n            <input type=\"number\" id=\"flot-feed-grade\" value=\"0.92\" min=\"0.01\" max=\"60\" step=\"0.05\" style=\"width: 100%; padding: 0.5rem; border: 1px solid var(--border); border-radius: 4px; background: var(--bg); color: var(--text);\">\n          </div>\n          <div>\n            <label style=\"display: block; font-size: 0.85rem; font-weight: 600; margin-bottom: 0.25rem;\">Target Conc Grade (%)</label>\n            <input type=\"number\" id=\"flot-conc-grade\" value=\"26.5\" min=\"0.1\" max=\"90\" step=\"0.5\" style=\"width: 100%; padding: 0.5rem; border: 1px solid var(--border); border-radius: 4px; background: var(--bg); color: var(--text);\">\n          </div>\n        </div>\n      </div>\n    </div>\n\n    <!-- Cell Geometry & Gas Holdup -->\n    <div style=\"background: var(--surface); padding: 1.25rem; border-radius: 8px; border: 1px solid var(--border);\">\n      <h3 style=\"margin-top: 0; font-size: 1.1rem; color: var(--text); border-bottom: 2px solid var(--border); padding-bottom: 0.5rem;\">2. Flotation Bank Geometry & Aeration</h3>\n      <div style=\"display: grid; gap: 1rem;\">\n        <div>\n          <label style=\"display: block; font-size: 0.85rem; font-weight: 600; margin-bottom: 0.25rem;\">Number of Cells in Bank (N)</label>\n          <input type=\"number\" id=\"flot-num-cells\" value=\"6\" min=\"1\" max=\"20\" step=\"1\" style=\"width: 100%; padding: 0.5rem; border: 1px solid var(--border); border-radius: 4px; background: var(--bg); color: var(--text);\">\n        </div>\n        <div>\n          <label style=\"display: block; font-size: 0.85rem; font-weight: 600; margin-bottom: 0.25rem;\">Nominal Cell Volume (m³ each)</label>\n          <input type=\"number\" id=\"flot-cell-vol\" value=\"70\" min=\"0.5\" max=\"600\" step=\"5\" style=\"width: 100%; padding: 0.5rem; border: 1px solid var(--border); border-radius: 4px; background: var(--bg); color: var(--text);\">\n        </div>\n        <div>\n          <label style=\"display: block; font-size: 0.85rem; font-weight: 600; margin-bottom: 0.25rem;\">Aeration Gas Holdup Fraction (ε_g, %)</label>\n          <input type=\"number\" id=\"flot-gas-holdup\" value=\"14\" min=\"2\" max=\"30\" step=\"1\" style=\"width: 100%; padding: 0.5rem; border: 1px solid var(--border); border-radius: 4px; background: var(--bg); color: var(--text);\">\n        </div>\n        <div>\n          <label style=\"display: block; font-size: 0.85rem; font-weight: 600; margin-bottom: 0.25rem;\">Scale-Up Dead Zone Factor (1.0 = ideal)</label>\n          <input type=\"number\" id=\"flot-scaleup-factor\" value=\"1.15\" min=\"1.0\" max=\"2.0\" step=\"0.05\" style=\"width: 100%; padding: 0.5rem; border: 1px solid var(--border); border-radius: 4px; background: var(--bg); color: var(--text);\">\n        </div>\n      </div>\n    </div>\n\n    <!-- Kelsall Kinetic Parameters -->\n    <div style=\"background: var(--surface); padding: 1.25rem; border-radius: 8px; border: 1px solid var(--border);\">\n      <h3 style=\"margin-top: 0; font-size: 1.1rem; color: var(--text); border-bottom: 2px solid var(--border); padding-bottom: 0.5rem;\">3. Kelsall Flotation Kinetics</h3>\n      <div style=\"display: grid; gap: 1rem;\">\n        <div>\n          <label style=\"display: block; font-size: 0.85rem; font-weight: 600; margin-bottom: 0.25rem;\">Slow-Floating Mass Fraction (Φ_slow, %)</label>\n          <input type=\"number\" id=\"flot-phi-slow\" value=\"22\" min=\"1\" max=\"95\" step=\"1\" style=\"width: 100%; padding: 0.5rem; border: 1px solid var(--border); border-radius: 4px; background: var(--bg); color: var(--text);\">\n        </div>\n        <div>\n          <label style=\"display: block; font-size: 0.85rem; font-weight: 600; margin-bottom: 0.25rem;\">Fast Rate Constant (k_fast, min⁻¹)</label>\n          <input type=\"number\" id=\"flot-k-fast\" value=\"1.75\" min=\"0.1\" max=\"10.0\" step=\"0.05\" style=\"width: 100%; padding: 0.5rem; border: 1px solid var(--border); border-radius: 4px; background: var(--bg); color: var(--text);\">\n        </div>\n        <div>\n          <label style=\"display: block; font-size: 0.85rem; font-weight: 600; margin-bottom: 0.25rem;\">Slow Rate Constant (k_slow, min⁻¹)</label>\n          <input type=\"number\" id=\"flot-k-slow\" value=\"0.14\" min=\"0.005\" max=\"2.0\" step=\"0.01\" style=\"width: 100%; padding: 0.5rem; border: 1px solid var(--border); border-radius: 4px; background: var(--bg); color: var(--text);\">\n        </div>\n        <div>\n          <label style=\"display: block; font-size: 0.85rem; font-weight: 600; margin-bottom: 0.25rem;\">Maximum Attainable Recovery (R_max, %)</label>\n          <input type=\"number\" id=\"flot-r-max\" value=\"95.0\" min=\"50\" max=\"100\" step=\"0.5\" style=\"width: 100%; padding: 0.5rem; border: 1px solid var(--border); border-radius: 4px; background: var(--bg); color: var(--text);\">\n        </div>\n      </div>\n    </div>\n  </div>\n\n  <!-- Real-Time Computed Diagnostic Dashboard -->\n  <div style=\"background: var(--surface); padding: 1.25rem; border-radius: 8px; border: 1px solid var(--border); margin-bottom: 1.5rem;\">\n    <h3 style=\"margin-top: 0; font-size: 1.1rem; color: var(--text); border-bottom: 2px solid var(--border); padding-bottom: 0.5rem;\">4. Metallurgical Balance & Kinetic Performance</h3>\n    <div style=\"display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 1rem; margin-bottom: 1rem;\">\n      <div style=\"background: var(--bg); padding: 1rem; border-radius: 6px; border: 1px solid var(--border);\">\n        <div style=\"font-size: 0.75rem; color: var(--text-muted); text-transform: uppercase; font-weight: 700;\">Overall Bank Recovery</div>\n        <div id=\"flot-out-rec\" style=\"font-size: 1.75rem; font-weight: 800; color: #10b981;\">--</div>\n        <div id=\"flot-out-rec-sub\" style=\"font-size: 0.8rem; color: var(--text-muted);\">Fast: -- | Slow: --</div>\n      </div>\n      <div style=\"background: var(--bg); padding: 1rem; border-radius: 6px; border: 1px solid var(--border);\">\n        <div style=\"font-size: 0.75rem; color: var(--text-muted); text-transform: uppercase; font-weight: 700;\">Residence Time (MRT)</div>\n        <div id=\"flot-out-mrt\" style=\"font-size: 1.75rem; font-weight: 800; color: #3b82f6;\">--</div>\n        <div id=\"flot-out-mrt-sub\" style=\"font-size: 0.8rem; color: var(--text-muted);\">Per Cell: -- | Total: --</div>\n      </div>\n      <div style=\"background: var(--bg); padding: 1rem; border-radius: 6px; border: 1px solid var(--border);\">\n        <div style=\"font-size: 0.75rem; color: var(--text-muted); text-transform: uppercase; font-weight: 700;\">Volumetric Slurry Flow</div>\n        <div id=\"flot-out-flow\" style=\"font-size: 1.75rem; font-weight: 800; color: #f59e0b;\">--</div>\n        <div id=\"flot-out-flow-sub\" style=\"font-size: 0.8rem; color: var(--text-muted);\">Pulp Density: -- kg/m³</div>\n      </div>\n      <div style=\"background: var(--bg); padding: 1rem; border-radius: 6px; border: 1px solid var(--border);\">\n        <div style=\"font-size: 0.75rem; color: var(--text-muted); text-transform: uppercase; font-weight: 700;\">Concentrate Mass Pull</div>\n        <div id=\"flot-out-pull\" style=\"font-size: 1.75rem; font-weight: 800; color: #8b5cf6;\">--</div>\n        <div id=\"flot-out-pull-sub\" style=\"font-size: 0.8rem; color: var(--text-muted);\">Tails Grade: -- %</div>\n      </div>\n    </div>\n  </div>\n\n  <!-- Dynamic Flotation Bank & Kinetic Recovery Canvas -->\n  <div style=\"background: var(--surface); padding: 1.25rem; border-radius: 8px; border: 1px solid var(--border); margin-bottom: 1.5rem;\">\n    <h3 style=\"margin-top: 0; font-size: 1.1rem; color: var(--text); border-bottom: 2px solid var(--border); padding-bottom: 0.5rem;\">5. Interactive Flotation Cell Bank & Cumulative Recovery Profile</h3>\n    <div style=\"position: relative; width: 100%; height: 320px;\">\n      <canvas id=\"flot-canvas\" style=\"width: 100%; height: 100%; display: block; border-radius: 4px; background: #0f172a;\"></canvas>\n    </div>\n  </div>\n\n  <!-- 5 Fatal Traps -->\n  <div style=\"margin-top: 2rem;\">\n    <h3 style=\"font-size: 1.3rem; margin-bottom: 1rem; color: var(--text);\">Fatal Engineering Traps & Industrial Pitfalls</h3>\n    <div style=\"display: grid; gap: 1rem;\">\n      <div class=\"trap-card\" style=\"background: var(--surface); padding: 1.25rem; border-radius: 8px; border-left: 4px solid #ef4444; border-top: 1px solid var(--border); border-right: 1px solid var(--border); border-bottom: 1px solid var(--border);\">\n        <h4 style=\"margin: 0 0 0.5rem; color: #ef4444; font-size: 1rem;\">1. Gas Holdup Displacement Blind Spot</h4>\n        <p style=\"margin: 0; font-size: 0.9rem; line-height: 1.5; color: var(--text-muted);\">\n          Aeration in industrial cells displaces 12% to 18% of tank volume with pressurized micro-bubbles. Calculating pulp retention time using geometric water volume rather than net aerated slurry volume (V_eff = V_tank * (1 - ε_g)) severely overestimates mineral-bubble collision contact time, causing massive recovery loss in downstream cells.\n        </p>\n      </div>\n      <div class=\"trap-card\" style=\"background: var(--surface); padding: 1.25rem; border-radius: 8px; border-left: 4px solid #f59e0b; border-top: 1px solid var(--border); border-right: 1px solid var(--border); border-bottom: 1px solid var(--border);\">\n        <h4 style=\"margin: 0 0 0.5rem; color: #f59e0b; font-size: 1rem;\">2. Hydraulic Gangue Entrainment Dilution</h4>\n        <p style=\"margin: 0; font-size: 0.9rem; line-height: 1.5; color: var(--text-muted);\">\n          True flotation relies on hydrophobic bubble attachment, but non-selective water recovery carries ultra-fine gangue (slimes, clays) directly across the froth lip. Operating cells with excessive air flow rates or shallow froth depths creates excessive water recovery, collapsing concentrate grade and overloading downstream cleaner circuits.\n        </p>\n      </div>\n      <div class=\"trap-card\" style=\"background: var(--surface); padding: 1.25rem; border-radius: 8px; border-left: 4px solid #10b981; border-top: 1px solid var(--border); border-right: 1px solid var(--border); border-bottom: 1px solid var(--border);\">\n        <h4 style=\"margin: 0 0 0.5rem; color: #10b981; font-size: 1rem;\">3. Froth Crowding & Coarse Particle Detachment</h4>\n        <p style=\"margin: 0; font-size: 0.9rem; line-height: 1.5; color: var(--text-muted);\">\n          In large flotation tanks (100 m³ to 600 m³), froth travel distance from impeller plume to launder lip is substantial. If launder lip loading exceeds 1.5 to 2.5 t/(h·m), froth stalls, bubble coalescence accelerates, and weakly attached coarse composite particles detach and fall back into the pulp, dropping coarse recovery by 20-30%.\n        </p>\n      </div>\n      <div class=\"trap-card\" style=\"background: var(--surface); padding: 1.25rem; border-radius: 8px; border-left: 4px solid #3b82f6; border-top: 1px solid var(--border); border-right: 1px solid var(--border); border-bottom: 1px solid var(--border);\">\n        <h4 style=\"margin: 0 0 0.5rem; color: #3b82f6; font-size: 1rem;\">4. Intercell Hydraulic Short-Circuiting & Backmixing</h4>\n        <p style=\"margin: 0; font-size: 0.9rem; line-height: 1.5; color: var(--text-muted);\">\n          Continuous flotation cells rely on tanks-in-series CSTR kinetics. Improperly baffled dart valves or open intercell weir slots permit significant bypass streams where newly introduced feed short-circuits directly into adjacent cells without entering the impeller shear zone, reducing effective mixing stages from N=6 to equivalent N=3.8.\n        </p>\n      </div>\n      <div class=\"trap-card\" style=\"background: var(--surface); padding: 1.25rem; border-radius: 8px; border-left: 4px solid #8b5cf6; border-top: 1px solid var(--border); border-right: 1px solid var(--border); border-bottom: 1px solid var(--border);\">\n        <h4 style=\"margin: 0 0 0.5rem; color: #8b5cf6; font-size: 1rem;\">5. Fast-Fraction Extinction in Scavenger Design</h4>\n        <p style=\"margin: 0; font-size: 0.9rem; line-height: 1.5; color: var(--text-muted);\">\n          Designers often fit a single global kinetic constant across an entire plant. However, after the first 2-3 rougher cells, the fast-floating fraction (k_fast) is 98% exhausted. The entering scavenger feed consists almost exclusively of the recalcitrant slow fraction (k_slow). Applying rougher kinetics to scavenger banks results in severely undersized scavenger volume.\n        </p>\n      </div>\n    </div>\n  </div>\n</div>\n\n<script>\n(function() {\n  const getVal = (id) => parseFloat(document.getElementById(id).value) || 0;\n\n  function calculate() {\n    const feedTph = getVal('flot-feed-rate');\n    const solidsWt = getVal('flot-solids-wt') / 100;\n    const sgS = getVal('flot-sg-solids');\n    const sgL = getVal('flot-sg-liquid');\n    const feedGrade = getVal('flot-feed-grade') / 100;\n    const concGrade = getVal('flot-conc-grade') / 100;\n\n    const numCells = Math.max(1, Math.round(getVal('flot-num-cells')));\n    const cellVol = getVal('flot-cell-vol');\n    const gasHoldup = getVal('flot-gas-holdup') / 100;\n    const scaleUp = getVal('flot-scaleup-factor');\n\n    const phiSlow = getVal('flot-phi-slow') / 100;\n    const phiFast = Math.max(0, 1 - phiSlow);\n    const kFast = getVal('flot-k-fast');\n    const kSlow = getVal('flot-k-slow');\n    const rMax = getVal('flot-r-max') / 100;\n\n    // Slurry Rheology & Volumetric Flow\n    const massWaterTph = solidsWt > 0 ? feedTph * (1 - solidsWt) / solidsWt : 0;\n    const volSolidsM3h = sgS > 0 ? feedTph / sgS : 0;\n    const volWaterM3h = sgL > 0 ? massWaterTph / sgL : 0;\n    const totalSlurryM3h = volSolidsM3h + volWaterM3h;\n    const totalSlurryLps = (totalSlurryM3h * 1000) / 3600;\n    const totalMassTph = feedTph + massWaterTph;\n    const pulpDensityKgM3 = totalSlurryM3h > 0 ? (totalMassTph / totalSlurryM3h) * 1000 : 0;\n\n    // Net Active Aerated Pulp Volume per cell\n    const netCellVol = cellVol * (1 - gasHoldup);\n    const effectiveCellVol = scaleUp > 0 ? netCellVol / scaleUp : netCellVol;\n\n    // Mean Residence Time (minutes)\n    const tauCellMin = totalSlurryM3h > 0 ? (effectiveCellVol / totalSlurryM3h) * 60 : 0;\n    const tauBankMin = tauCellMin * numCells;\n\n    // CSTR Tanks in Series Recovery Calculation\n    // R_comp = 1 - 1 / (1 + k * tau)^N\n    const cellRecoveries = [];\n    let prevFastRem = 1.0;\n    let prevSlowRem = 1.0;\n\n    for (let i = 1; i <= numCells; i++) {\n      const remFast = 1 / Math.pow(1 + kFast * tauCellMin, i);\n      const remSlow = 1 / Math.pow(1 + kSlow * tauCellMin, i);\n      const cumRecFast = (1 - remFast);\n      const cumRecSlow = (1 - remSlow);\n      const cumTotalRec = rMax * (phiFast * cumRecFast + phiSlow * cumRecSlow);\n      cellRecoveries.push({\n        cell: i,\n        cumRec: cumTotalRec * 100,\n        cumFast: cumRecFast * 100,\n        cumSlow: cumRecSlow * 100\n      });\n    }\n\n    const finalBankRec = cellRecoveries[cellRecoveries.length - 1].cumRec;\n    const finalFastRec = cellRecoveries[cellRecoveries.length - 1].cumFast;\n    const finalSlowRec = cellRecoveries[cellRecoveries.length - 1].cumSlow;\n\n    // Metallurgical Balances\n    const valuableFeedTph = feedTph * feedGrade;\n    const valuableConcTph = valuableFeedTph * (finalBankRec / 100);\n    const concTph = concGrade > 0 ? valuableConcTph / concGrade : 0;\n    const massPullPct = feedTph > 0 ? (concTph / feedTph) * 100 : 0;\n    const tailsTph = Math.max(0, feedTph - concTph);\n    const valuableTailsTph = Math.max(0, valuableFeedTph - valuableConcTph);\n    const tailsGradePct = tailsTph > 0 ? (valuableTailsTph / tailsTph) * 100 : 0;\n\n    // Update UI Elements\n    document.getElementById('flot-out-rec').textContent = finalBankRec.toFixed(2) + '%';\n    document.getElementById('flot-out-rec-sub').textContent = 'Fast: ' + finalFastRec.toFixed(1) + '% | Slow: ' + finalSlowRec.toFixed(1) + '%';\n\n    document.getElementById('flot-out-mrt').textContent = tauBankMin.toFixed(1) + ' min';\n    document.getElementById('flot-out-mrt-sub').textContent = 'Per Cell: ' + tauCellMin.toFixed(2) + ' min | Bank: ' + tauBankMin.toFixed(1) + ' min';\n\n    document.getElementById('flot-out-flow').textContent = Math.round(totalSlurryM3h).toLocaleString() + ' m³/h';\n    document.getElementById('flot-out-flow-sub').textContent = 'Density: ' + Math.round(pulpDensityKgM3) + ' kg/m³ (' + totalSlurryLps.toFixed(1) + ' L/s)';\n\n    document.getElementById('flot-out-pull').textContent = massPullPct.toFixed(2) + '%';\n    document.getElementById('flot-out-pull-sub').textContent = 'Conc: ' + concTph.toFixed(1) + ' t/h | Tails: ' + tailsGradePct.toFixed(3) + '%';\n\n    drawCanvas(cellRecoveries, numCells, finalBankRec, tauBankMin);\n\n    window.flotDiagnostic = {\n      feedTph, solidsWt: solidsWt * 100, feedGrade: feedGrade * 100, concGrade: concGrade * 100,\n      totalSlurryM3h, pulpDensityKgM3, numCells, cellVol, tauCellMin, tauBankMin,\n      finalBankRec, finalFastRec, finalSlowRec, massPullPct, concTph, tailsTph, tailsGradePct\n    };\n  }\n\n  function drawCanvas(recoveries, numCells, finalRec, totalTime) {\n    const canvas = document.getElementById('flot-canvas');\n    if (!canvas) return;\n    const ctx = canvas.getContext('2d');\n    const dpr = window.devicePixelRatio || 1;\n    const w = canvas.clientWidth;\n    const h = canvas.clientHeight;\n    canvas.width = w * dpr;\n    canvas.height = h * dpr;\n    ctx.scale(dpr, dpr);\n\n    ctx.clearRect(0, 0, w, h);\n\n    // Layout: Left half = Bank cross-section; Right half = Cumulative recovery curve\n    const splitX = Math.floor(w * 0.52);\n\n    // 1. Draw Flotation Cells on Left\n    ctx.fillStyle = '#1e293b';\n    ctx.font = '11px sans-serif';\n    ctx.textAlign = 'center';\n\n    const cellAreaW = splitX - 40;\n    const cellAreaH = h - 60;\n    const cellW = Math.min(65, Math.floor(cellAreaW / numCells) - 8);\n    const startX = 25;\n    const cellH = 120;\n    const cellY = 70;\n\n    ctx.fillStyle = '#94a3b8';\n    ctx.font = 'bold 12px sans-serif';\n    ctx.textAlign = 'left';\n    ctx.fillText('Flotation Cell Bank (' + numCells + ' Cells in Series)', startX, 30);\n\n    for (let i = 0; i < numCells; i++) {\n      const cx = startX + i * (cellW + 8);\n\n      // Tank body\n      ctx.fillStyle = '#1e293b';\n      ctx.strokeStyle = '#475569';\n      ctx.lineWidth = 2;\n      ctx.beginPath();\n      ctx.rect(cx, cellY, cellW, cellH);\n      ctx.fill();\n      ctx.stroke();\n\n      // Slurry level\n      const slurryH = cellH * 0.75;\n      const slurryY = cellY + cellH - slurryH;\n      const recFrac = recoveries[i] ? recoveries[i].cumRec / 100 : 0;\n      // Gradient: darker blue to lighter green as valuable mineral is removed\n      ctx.fillStyle = '#0369a1';\n      ctx.fillRect(cx + 2, slurryY, cellW - 4, slurryH - 2);\n\n      // Froth layer on top\n      const frothH = cellH * 0.15;\n      const frothY = slurryY - frothH;\n      ctx.fillStyle = '#e2e8f0';\n      ctx.fillRect(cx + 2, frothY, cellW - 4, frothH);\n\n      // Impeller shaft & rotor\n      ctx.strokeStyle = '#94a3b8';\n      ctx.lineWidth = 2;\n      ctx.beginPath();\n      ctx.moveTo(cx + cellW / 2, cellY);\n      ctx.lineTo(cx + cellW / 2, cellY + cellH - 18);\n      ctx.stroke();\n\n      // Rotor blades\n      ctx.fillStyle = '#cbd5e1';\n      ctx.fillRect(cx + cellW / 2 - 12, cellY + cellH - 20, 24, 6);\n\n      // Air bubbles in pulp\n      ctx.fillStyle = 'rgba(255, 255, 255, 0.6)';\n      for (let b = 0; b < 4; b++) {\n        const bx = cx + 8 + (b * 11) % (cellW - 16);\n        const by = cellY + cellH - 35 - b * 16;\n        ctx.beginPath();\n        ctx.arc(bx, by, 2.5, 0, Math.PI * 2);\n        ctx.fill();\n      }\n\n      // Cell index label\n      ctx.fillStyle = '#f8fafc';\n      ctx.font = '10px sans-serif';\n      ctx.textAlign = 'center';\n      ctx.fillText('C' + (i + 1), cx + cellW / 2, cellY + cellH + 15);\n      if (recoveries[i]) {\n        ctx.fillStyle = '#38bdf8';\n        ctx.fillText(recoveries[i].cumRec.toFixed(1) + '%', cx + cellW / 2, cellY + cellH + 28);\n      }\n    }\n\n    // Divider line\n    ctx.strokeStyle = '#334155';\n    ctx.lineWidth = 1;\n    ctx.beginPath();\n    ctx.moveTo(splitX, 20);\n    ctx.lineTo(splitX, h - 20);\n    ctx.stroke();\n\n    // 2. Draw Recovery Plot on Right\n    const plotX = splitX + 50;\n    const plotY = 40;\n    const plotW = w - plotX - 30;\n    const plotH = h - 85;\n\n    ctx.fillStyle = '#94a3b8';\n    ctx.font = 'bold 12px sans-serif';\n    ctx.textAlign = 'left';\n    ctx.fillText('Cumulative Recovery Curve (%)', plotX, 25);\n\n    // Axes\n    ctx.strokeStyle = '#475569';\n    ctx.lineWidth = 1.5;\n    ctx.beginPath();\n    ctx.moveTo(plotX, plotY);\n    ctx.lineTo(plotX, plotY + plotH);\n    ctx.lineTo(plotX + plotW, plotY + plotH);\n    ctx.stroke();\n\n    // Grid lines & Y labels (0 to 100%)\n    ctx.fillStyle = '#64748b';\n    ctx.font = '10px sans-serif';\n    ctx.textAlign = 'right';\n    for (let yPct = 0; yPct <= 100; yPct += 25) {\n      const gy = plotY + plotH - (yPct / 100) * plotH;\n      ctx.fillText(yPct + '%', plotX - 6, gy + 3);\n      if (yPct > 0) {\n        ctx.strokeStyle = '#1e293b';\n        ctx.beginPath();\n        ctx.moveTo(plotX, gy);\n        ctx.lineTo(plotX + plotW, gy);\n        ctx.stroke();\n      }\n    }\n\n    // Plot Points & Line\n    ctx.strokeStyle = '#10b981';\n    ctx.lineWidth = 2.5;\n    ctx.beginPath();\n    ctx.moveTo(plotX, plotY + plotH);\n\n    recoveries.forEach((r, idx) => {\n      const px = plotX + ((idx + 1) / numCells) * plotW;\n      const py = plotY + plotH - (r.cumRec / 100) * plotH;\n      ctx.lineTo(px, py);\n    });\n    ctx.stroke();\n\n    // Draw markers\n    recoveries.forEach((r, idx) => {\n      const px = plotX + ((idx + 1) / numCells) * plotW;\n      const py = plotY + plotH - (r.cumRec / 100) * plotH;\n      ctx.fillStyle = '#10b981';\n      ctx.beginPath();\n      ctx.arc(px, py, 4.5, 0, Math.PI * 2);\n      ctx.fill();\n      ctx.strokeStyle = '#f8fafc';\n      ctx.lineWidth = 1.5;\n      ctx.stroke();\n\n      // X label\n      ctx.fillStyle = '#94a3b8';\n      ctx.textAlign = 'center';\n      ctx.fillText('C' + (idx + 1), px, plotY + plotH + 16);\n    });\n  }\n\n  // Setup Event Listeners\n  const inputs = [\n    'flot-feed-rate', 'flot-solids-wt', 'flot-sg-solids', 'flot-sg-liquid',\n    'flot-feed-grade', 'flot-conc-grade', 'flot-num-cells', 'flot-cell-vol',\n    'flot-gas-holdup', 'flot-scaleup-factor', 'flot-phi-slow', 'flot-k-fast',\n    'flot-k-slow', 'flot-r-max'\n  ];\n\n  inputs.forEach(id => {\n    const el = document.getElementById(id);\n    if (el) {\n      el.addEventListener('input', calculate);\n    }\n  });\n\n  const copyBtn = document.getElementById('btn-copy-flot');\n  if (copyBtn) {\n    copyBtn.addEventListener('click', function() {\n      if (!window.flotDiagnostic) return;\n      const d = window.flotDiagnostic;\n      const text = [\n        '========================================',\n        'FROTH FLOTATION BANK DIAGNOSTIC REPORT',\n        '========================================',\n        'Ore Feed Rate: ' + d.feedTph + ' t/h (' + d.solidsWt.toFixed(1) + '% solids by weight)',\n        'Volumetric Slurry Flow: ' + Math.round(d.totalSlurryM3h) + ' m³/h',\n        'Slurry Pulp Density: ' + Math.round(d.pulpDensityKgM3) + ' kg/m³',\n        'Bank Geometry: ' + d.numCells + ' cells @ ' + d.cellVol + ' m³ nominal each',\n        'Mean Residence Time: ' + d.tauBankMin.toFixed(1) + ' min total (' + d.tauCellMin.toFixed(2) + ' min/cell)',\n        '----------------------------------------',\n        'METALLURGICAL PERFORMANCE:',\n        'Overall Bank Recovery: ' + d.finalBankRec.toFixed(2) + '%',\n        'Fast Fraction Recovery: ' + d.finalFastRec.toFixed(1) + '%',\n        'Slow Fraction Recovery: ' + d.finalSlowRec.toFixed(1) + '%',\n        'Concentrate Mass Pull: ' + d.massPullPct.toFixed(2) + '% (' + d.concTph.toFixed(1) + ' t/h conc @ ' + d.concGrade.toFixed(2) + '% grade)',\n        'Tailings Rate: ' + d.tailsTph.toFixed(1) + ' t/h (Tails Grade: ' + d.tailsGradePct.toFixed(3) + '%)',\n        '========================================',\n        'Generated by Digital Tools Shed Flotation Simulator (Kelsall Two-Rate CSTR Model)'\n      ].join('\\n');\n\n      navigator.clipboard.writeText(text).then(() => {\n        const originalText = copyBtn.innerHTML;\n        copyBtn.innerHTML = '<span>✓</span> Diagnostic Summary Copied to Clipboard!';\n        copyBtn.style.color = '#10b981';\n        copyBtn.style.borderColor = '#10b981';\n        setTimeout(() => {\n          copyBtn.innerHTML = originalText;\n          copyBtn.style.color = 'var(--text)';\n          copyBtn.style.borderColor = 'var(--border)';\n        }, 2500);\n      });\n    });\n  }\n\n  calculate();\n  window.addEventListener('resize', calculate);\n})();\n</script>\n"
}